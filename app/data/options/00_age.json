{
  "groups": [
    {
      "id": "age",
      "label": "Age",
      "kind": "number",
      "field": "age",
      "order": 1,
      "min": 20,
      "max": 10000,
      "step": 1,
      "default": 25,
      "unit": "years",
      "_note": "prompt_ranges removed at 5.6c (V2 A3: age is img:none and renders via apparent_age only; the structural 'adult' anchor stays in prompt.py). The numeric machinery stays dormant in the format, still guarding age."
    }
  ]
}
